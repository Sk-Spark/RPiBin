# Add to the end of .bashrc file
#----sk---
export PATH=/home/spark/bin/RPiBin:$PATH
alias updg='sudo apt update -y && sudo apt upgrade -y' 
alias upd='sudo apt update -y' 
alias upg='sudo apt upgrade -y'
